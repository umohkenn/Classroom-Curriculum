[project]
name = "curriculum_classroom"
authors = [{ name = "Curriculum Classroom" }]
description = "AI-assisted curriculum-to-classroom suite for Frappe/ERPNext Education"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0"
